apiVersion: v1
kind: Service
metadata:
  name: chainlit-service
spec:
  type: NodePort
  selector:
    app: chainlit-bot
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 31170  # Optional: specify or let Kubernetes assign a free port
